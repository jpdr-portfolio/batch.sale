package com.challenge.acc.batch.sale.repository;

import com.challenge.acc.batch.sale.model.SaleMaster;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SalesMasterRepository extends JpaRepository<SaleMaster, Integer> {
}
